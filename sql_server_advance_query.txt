//create database
DECLARE @DatabaseName VARCHAR(45) = 'DynamicDB';
DECLARE @SQL VARCHAR(45);

SET @SQL = 'CREATE DATABASE [' + @DatabaseName + ']';
EXEC(@SQL);

//create create table

DECLARE @TableName VARCHAR(45) = 'Users';
DECLARE @SQL VARCHAR(45);

SET @SQL = '
CREATE TABLE [' + @TableName + '] (
    Id INT ,
    Name NVARCHAR(100),
)';

EXEC(@SQL);

select * from Users

//insert  
DECLARE @createtable NVARCHAR(50) = 'students';
declare @id INT=2;
declare @name varchar(45)='soomro';
DECLARE @SQL NVARCHAR(MAX);

SET @SQL = 'insert into [' + @createtable + '](id,name)values(@id,@name);';

EXEC sp_executesql
  @SQL,
  N'@id INT,@name VARCHAR(45)', 
  @id= @id,
  @name=@name;





 exec sp_rename 'user','users'
 EXEC sp_help 'Users';


select name
from sys.databases

select name
from sys.tables


select name
from sys.
triggers

select name
from sys.procedures



