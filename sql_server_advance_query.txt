
DECLARE @DatabaseName VARCHAR(45) = 'DynamicDB';
DECLARE @SQL VARCHAR(45);

SET @SQL = 'CREATE DATABASE [' + @DatabaseName + ']';
EXEC(@SQL);



DECLARE @TableName VARCHAR(45) = 'Users';
DECLARE @SQL VARCHAR(45);

SET @SQL = '
CREATE TABLE [' + @TableName + '] (
    Id INT IDENTITY(1,1) PRIMARY KEY,
    Name NVARCHAR(100),
    Email NVARCHAR(100)
)';

EXEC(@SQL);

select * from Users

DECLARE @TableName VARCHAR(45) = 'Users';
DECLARE @Name VARCHAR(45) = 'John';
DECLARE @Email VARCHAR(45) = 'john@example.com';
DECLARE @SQL VARCHAR(MAX);

SET @SQL = '
INSERT INTO [' + @TableName + '] (Name, Email)
VALUES (@Name, @Email)';


EXEC sp_executesql 
    @SQL,
    N'@Name NVARCHAR(100), @Email NVARCHAR(100)',
    @Name=@Name,
    @Email=@Email;

 exec sp_rename 'user','users'
 EXEC sp_help 'Users';


select name
from sys.databases

select name
from sys.tables


select name
from sys.
triggers

select name
from sys.procedures


